- breadcrumb :payments
= link_to t('shared.add_new_item'), new_payment_path, class: 'btn btn-default'
%table.tableP.table-condensed
  %thead
    %tr
      %th= t('payments.columns.student')
      %th= t('payments.columns.value')
      %th= t('payments.columns.date')
      %th= t('shared.actions')
  %tbody
    = render payments